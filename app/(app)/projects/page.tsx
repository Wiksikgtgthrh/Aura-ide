// Content is rendered inside AppContentArea's Activity shell (app-content-area.tsx).
// Returning null here avoids a duplicate RSC render on navigation.
export default function ProjectsPage() {
  return null
}
