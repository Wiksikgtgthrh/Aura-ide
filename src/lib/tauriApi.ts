// Placeholder — этот файл перезаписывается на каждом `pnpm tauri dev` через
// tauri-specta (см. src-tauri/src/lib.rs → build_specta). Не редактировать
// вручную. Пока IDE не была собрана хотя бы раз, здесь пусто.
export {};
